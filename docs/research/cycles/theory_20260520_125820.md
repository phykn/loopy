# Loopy Theory Cycle 20260520_125820

## Target

Test the current Loopy main theory within the user request: run the theory loop, not just structural validation.

## Claim

The current `THEORY_LOOPY.md` is stable enough as Loopy's main theory.

## Evidence

- The final theory is short and centered on one shape: `Loopy = Loop(Unstable -> Tested -> Stable)`.
- It keeps the important rejection boundary: "The test must be able to reject it."
- It separates Loopy from checklist completion: "A checklist completes steps. Loopy promotes survivors."
- The latest refine critic gave `THEORY_LOOPY.md` grade A with no weaknesses.

## Objection

The `Minimum` section has one weak phrase:

```text
a loop where rejection can change it
```

This overlaps with:

```text
a test that can reject the change
```

It also makes "loop" less beautiful than the rest of the theory. The core does not need both "rejection can change it" and "test can reject the change."

## Revision

Make the loop condition simpler:

```text
- a loop that can change it;
- a test that can reject the change;
```

The rejection boundary remains in the test. The loop only needs to be capable of changing the unstable artifact.

## Revision target: theory or skill/process

Theory.

## Why It Is Stronger

The revision removes duplicated rejection language without weakening the boundary.

The minimum condition becomes easier to read:

- artifact starts unstable;
- loop can change it;
- test can reject the change;
- stable artifact contains only survivors.

## Decision

Promote.

## Progress toward target

This is a small but real theory improvement because it changes the minimum condition wording and makes the final theory simpler.

## Next question

Can `THEORY_LOOPY.md` now stop, or does the boundary need an example?
