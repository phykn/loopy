# OpenAI interface metadata only. Role behavior lives in SKILL.md.
interface:
  display_name: "Loopy"
  short_description: "Improve work through counterexamples"
  default_prompt: "Use $loopy to improve this until all important in-scope counterexamples are resolved, blocked, or out of scope."
