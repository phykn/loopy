# OpenAI interface metadata only. Role behavior lives in SKILL.md.
interface:
  display_name: "Loopy Theory"
  short_description: "Run evidence-backed theory refinement cycles"
  default_prompt: "Use $loopy-theory to refine this idea into a stronger theory."
